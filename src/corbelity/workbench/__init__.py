"""Corbelity Model Workbench: a browser UI over corbelity.model_client.

Run it with `corbelity-workbench` (or `python -m corbelity.workbench`). The FastAPI
application itself is `corbelity.workbench.app:app`.
"""

__version__ = "0.1.0"
